<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ./partials/_header %>
</head>
<body>
    <!-- Add your EJS code here -->
    <h1>Your URLS</h1>
      <ul>
        <% Object.keys(urls).forEach(function(key) { %>
          <li> <%=key%> </a> - <%= urls[key] %></li>
        <% }); %>
      </ul>
    <a href="">Click here to shorten your own link.</a>
</body>
</html>
