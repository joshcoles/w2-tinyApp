<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
    <h2>Urls, Baby!!!</h2>
      <ul>
        <% Object.keys(urls).forEach(function(key) { %>
          <li><a href="<%= key %>"> <%=key%> </a> - <%= urls[key] %></li>
        <% }); %>
      </ul>
</body>
</html>
